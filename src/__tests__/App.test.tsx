import { render } from "@testing-library/react";
import App from "../App";

test("Main page corrtectly rendered", () => {
  // setup
  render(<App />);

  // Init

  // Pre espectations

  // Post Expectations
  expect(true).toBeTruthy();
});
